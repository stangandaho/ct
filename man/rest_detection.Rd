% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{rest_detection}
\alias{rest_detection}
\title{Simulated camera-trap detections for the REST / RAD-REST workflow}
\format{
A tibble with one row per video (detection) and columns:
\itemize{
\item \code{Station}: Camera station ID.
\item \code{Species}: Detected species name.
\item \code{DateTime}: Capture time as a \code{"YYYY-MM-DD HH:MM:SS"} string.
\item \code{y}: Number of passes through the focal area in that video
(\code{NA} for background species).
\item \code{Stay}: Staying time within the focal area in seconds
(\code{NA} when the animal did not enter).
\item \code{Cens}: Right-censoring flag for \code{Stay} (1 = censored, 0 = observed).
}
}
\usage{
rest_detection
}
\description{
A small simulated dataset illustrating the inputs needed by \code{\link[=ct_fit_rest]{ct_fit_rest()}}
and the \verb{ct_rest_*} preparation helpers. It contains detections of a focal
species (\code{"Red duiker"}) recorded at 8 stations over roughly one month,
together with two background species.
}
\examples{
data(rest_detection)
head(rest_detection)
}
\seealso{
\link{rest_station}, \code{\link[=ct_fit_rest]{ct_fit_rest()}}
}
\keyword{datasets}
