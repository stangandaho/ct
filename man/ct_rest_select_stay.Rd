% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct_rest_select_stay.R
\name{ct_rest_select_stay}
\alias{ct_rest_select_stay}
\title{Choose a staying-time distribution for REST by WAIC}
\usage{
ct_rest_select_stay(
  stay_data,
  species,
  stay_formula = Stay ~ 1,
  stay_distribution = c("lognormal", "gamma", "weibull", "exponential"),
  stay_random_effect = NULL,
  compare_models = FALSE,
  iterations = 5000,
  burnin = 1000,
  thin = 4,
  chains = 3,
  cores = 3,
  quiet = FALSE
)
}
\arguments{
\item{stay_data}{Staying-time data from \code{\link[=ct_rest_stay]{ct_rest_stay()}}.}

\item{species}{Single species name to analyse.}

\item{stay_formula}{Staying-time formula, e.g. \code{Stay ~ 1} or \code{Stay ~ 1 + habitat}.}

\item{stay_distribution}{One or more of \code{"lognormal"}, \code{"gamma"}, \code{"weibull"},
\code{"exponential"} to compare.}

\item{stay_random_effect}{Optional column in \code{stay_data} for a random effect
on staying time. Tidy-selected (string, bare name, or position).}

\item{compare_models}{If \code{TRUE}, also compare every covariate combination of
\code{stay_formula}.}

\item{iterations, burnin, thin, chains, cores}{MCMC settings.}

\item{quiet}{If \code{TRUE}, suppress progress messages.}
}
\value{
An object of class \code{ct_rest_stay} with a \code{waic} ranking tibble, a
\code{summary} of the mean staying time for the best model, and its \code{samples}.
}
\description{
Fits the REST staying-time survival sub-model under one or more candidate
distributions (and, optionally, covariate combinations) and ranks them by
WAIC, with a Bayesian p-value as a goodness-of-fit check. Use the winning
\code{stay_distribution} in \code{\link[=ct_fit_rest]{ct_fit_rest()}}.
}
\examples{
data(rest_detection)
data(rest_station)

stay <- ct_rest_stay(rest_detection, rest_station)

\dontrun{
# Compare candidate staying-time distributions by WAIC (requires 'nimble')
ct_rest_select_stay(
  stay, species = "Red duiker",
  stay_distribution = c("lognormal", "gamma", "weibull"),
  iterations = 3000, burnin = 1000, chains = 2, cores = 2
)
}
}
\seealso{
\code{\link[=ct_fit_rest]{ct_fit_rest()}}
}
