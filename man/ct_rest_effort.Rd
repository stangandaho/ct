% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct_rest_data.R
\name{ct_rest_effort}
\alias{ct_rest_effort}
\title{Add camera-trapping effort (days) to formatted station data}
\usage{
ct_rest_effort(
  detection_data,
  station_data,
  station_column = "Station",
  datetime_column = "DateTime",
  term_column = NULL,
  plot = FALSE
)
}
\arguments{
\item{detection_data}{A data frame with one row per detection.}

\item{station_data}{A data frame from \code{\link[=ct_rest_passes]{ct_rest_passes()}} (must have a
\code{Station} column).}

\item{station_column, datetime_column}{Columns for the station ID and datetime
in \code{detection_data}.}

\item{term_column}{Optional column identifying survey terms;
\code{NULL} to ignore.}

\item{plot}{If \code{TRUE}, draw a Gantt-style plot of operation periods.}
}
\value{
\code{station_data} with an added \code{Effort} column (days). Stations with no
or zero effort are dropped with a warning.
}
\description{
Effort is approximated as the span between the first and last detection. When
\code{term_col} is given, effort is computed per survey term and summed per station
so inactive gaps between terms are not counted.
}
\examples{
data(rest_detection)
data(rest_station)

stations <- ct_rest_passes(rest_detection, rest_station, model = "REST")
ct_rest_effort(rest_detection, stations)

}
