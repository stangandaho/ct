% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct_fit_rest.R
\name{ct_fit_rest}
\alias{ct_fit_rest}
\title{Fit the Random Encounter and Staying Time (REST / RAD-REST) model}
\usage{
ct_fit_rest(
  stay_data,
  station_data,
  activity_data,
  species,
  focal_area,
  model = c("REST", "RAD-REST"),
  stay_formula = Stay ~ 1,
  density_formula = ~1,
  passes_formula = ~1,
  stay_random_effect = NULL,
  stay_distribution = c("lognormal", "gamma", "weibull", "exponential"),
  activity_method = c("kernel", "mixture"),
  bandwidth_adjust = 1,
  mixture_components = 10,
  compare_models = FALSE,
  iterations = 5000,
  burnin = 1000,
  thin = 2,
  chains = 3,
  cores = 3,
  quiet = FALSE
)
}
\arguments{
\item{stay_data}{Staying-time data, e.g. the output of \code{\link[=ct_rest_stay]{ct_rest_stay()}}, with
columns \code{Station}, \code{Species}, \code{Stay} (seconds) and \code{Cens} (1 = censored,
0 = fully observed).}

\item{station_data}{Per-station encounter and effort data, e.g. the output of
\code{\link[=ct_rest_effort]{ct_rest_effort()}}. For \code{model = "REST"} it must contain \code{Station},
\code{Species}, \code{Effort} (days) and \code{Y} (passes). For \code{model = "RAD-REST"} it
must instead contain \code{N} (videos) and the \code{y_0}, \code{y_1}, ... pass-count
columns.}

\item{activity_data}{Detection times in radians, e.g. the output of
\code{\link[=ct_rest_activity]{ct_rest_activity()}}, with columns \code{Species} and \code{time}.}

\item{species}{Single species name to analyse (must appear in the data).}

\item{focal_area}{Focal-area size in square metres. Either a single number
(the same area at every camera) or the name of a column in \code{station_data}
giving a camera-specific focal area per station.}

\item{model}{Either \code{"REST"} or \code{"RAD-REST"}.}

\item{stay_formula}{Model formula for staying time. The left-hand side names
the staying-time column, e.g. \code{Stay ~ 1} or \code{Stay ~ 1 + habitat}.}

\item{density_formula}{One-sided formula for density covariates, e.g. \code{~ 1}
or \code{~ habitat}. Density is latent, so the left-hand side is omitted.}

\item{passes_formula}{One-sided formula for the number of passes. Used only
when \code{model = "RAD-REST"}; ignored otherwise.}

\item{stay_random_effect}{Optional column in \code{stay_data} giving a random
effect on staying time. Default \code{NULL} (no random effect).}

\item{stay_distribution}{Distribution for staying time: one of \code{"lognormal"},
\code{"gamma"}, \code{"weibull"} or \code{"exponential"}. Ideally chosen with
\code{\link[=ct_rest_select_stay]{ct_rest_select_stay()}}.}

\item{activity_method}{How to estimate the activity proportion: \code{"kernel"}
(fixed kernel density) or \code{"mixture"} (Bayesian von Mises mixture).}

\item{bandwidth_adjust}{Bandwidth multiplier for \code{activity_method = "kernel"}.}

\item{mixture_components}{Maximum number of von Mises components for
\code{activity_method = "mixture"}.}

\item{compare_models}{If \code{TRUE}, fit every combination of the density
covariates and rank them by WAIC. If \code{FALSE}, fit only \code{density_formula}.}

\item{iterations, burnin, thin, chains, cores}{MCMC settings: total iterations
per chain, burn-in length, thinning interval, number of chains and CPU
cores for parallel sampling.}

\item{quiet}{If \code{TRUE}, suppress progress messages.}
}
\value{
An object of class \code{ct_rest} (a list) with:
\describe{
\item{\code{waic}}{A tibble ranking the candidate density models by WAIC.}
\item{\code{summary}}{A tibble of posterior summaries for density (individuals
per km^2), mean staying time and, for RAD-REST, the mean number of passes.}
\item{\code{samples}}{A \code{coda::mcmc.list} of posterior draws for the best model.}
\item{\code{activity_curve}}{(mixture only) the estimated activity density curve.}
}
}
\description{
Estimates animal density from camera-trap data \strong{without individual
recognition} using the Random Encounter and Staying Time (REST) model of
Nakashima, Fukasawa & Samejima (2018) and its RAD-REST extension
(Nakashima et al. 2026). Parameters are estimated in a Bayesian framework
by MCMC sampling with \pkg{nimble}.
}
\details{
\subsection{The idea behind REST}{

A camera watches a small \emph{focal area} of known size in front of the lens.
If we know (i) how often animals pass through that area, (ii) how long they
stay in it on average, and (iii) the fraction of the day they are active,
density follows from a simple flow argument. Intuitively, the expected
number of detected passes is

\deqn{E[Y] = D \times S \times T \times p_{act} / \bar{t}}

where \eqn{D} is density, \eqn{S} the focal-area size, \eqn{T} the survey
duration, \eqn{p_{act}} the activity proportion and \eqn{\bar{t}} the mean
staying time. Re-arranging gives the density estimator
\eqn{D = Y\,\bar{t} / (S\,T\,p_{act})}. \code{ct_fit_rest()} fits every piece of
this equation jointly so that uncertainty propagates into the density
estimate.

Three sub-models are combined:
\itemize{
\item \strong{Staying time} (\code{stay_data}): a survival model. Animals still in
the focal area when the video ends are \emph{right-censored}; the chosen
\code{stay_distribution} (lognormal/gamma/weibull/exponential) handles this.
\item \strong{Encounters} (\code{station_data}): the number of passes \code{Y} per station
is modelled as negative-binomial (REST), or, for RAD-REST, the number of
videos showing 0,1,2,... passes is modelled with a Dirichlet-multinomial
so that miscounting of passes is accounted for.
\item \strong{Activity} (\code{activity_data}): the active fraction of the day,
estimated either by kernel density (Rowcliffe et al. 2014) or a Bayesian
von Mises mixture (Nakashima et al. 2026).
}
}
}
\examples{
data(rest_detection)
data(rest_station)

# 1. Build the three inputs from raw detections (these steps run quickly)
stay <- ct_rest_stay(rest_detection, rest_station)
stations <- ct_rest_passes(rest_detection, rest_station, model = "REST")
stations <- ct_rest_effort(rest_detection, stations)
activity <- ct_rest_activity(rest_detection)

\dontrun{
# 2. Fit REST for the focal species (requires the 'nimble' package)
fit <- ct_fit_rest(
  stay_data = stay,
  station_data  = stations,
  activity_data = activity,
  species = "Red duiker",
  focal_area = 3.0, # focal-area size in m^2
  model = "REST",
  stay_distribution = "lognormal",
  iterations = 3000, burnin = 1000, chains = 2, cores = 2
)
fit
fit$summary   # density (individuals per km^2) and mean staying time

# RAD-REST instead: use pass-classified station data
stations_rad <- ct_rest_effort(
  detection_data = rest_detection,
  station_data = ct_rest_passes(rest_detection, rest_station, model = "RAD-REST")
)

fit_rad <- ct_fit_rest(
  stay_data = stay,
  station_data  = stations_rad,
  activity_data = activity,
  species = "Red duiker",
  focal_area = 3.0,
  model = "RAD-REST"
)
}

}
\references{
Nakashima, Y., Fukasawa, K. & Samejima, H. (2018) Estimating animal density
without individual recognition using information derived from camera traps.
\emph{Journal of Applied Ecology}, 55, 735-744.

Nakashima, Y. et al. (2026) Reducing data-processing effort in camera-trap
density estimation: extending the REST model. \emph{Methods in Ecology and
Evolution}.
}
\seealso{
\code{\link[=ct_rest_stay]{ct_rest_stay()}}, \code{\link[=ct_rest_effort]{ct_rest_effort()}}, \code{\link[=ct_rest_activity]{ct_rest_activity()}},
\code{\link[=ct_rest_select_stay]{ct_rest_select_stay()}}
}
