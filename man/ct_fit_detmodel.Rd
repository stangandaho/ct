% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/REM.R
\name{ct_fit_detmodel}
\alias{ct_fit_detmodel}
\title{Fit animal detection}
\usage{
ct_fit_detmodel(
  formula,
  data,
  newdata = NULL,
  unit = c("m", "km", "cm", "degree", "radian"),
  ...
)
}
\arguments{
\item{formula}{A formula specifying the response (e.g., \code{radius ~ 1} or \code{angle ~ covariate}).}

\item{data}{A data frame containing detection observations.}

\item{newdata}{Optional new data frame with covariate values for prediction.}

\item{unit}{Unit of the detection variable. One of \code{"m"}, \code{"km"}, \code{"cm"} for distance,
or \code{"degree"}, \code{"radian"} for angle.}

\item{...}{Additional arguments passed to \code{\link[Distance:ds]{Distance::ds()}}.}
}
\value{
a list with elements:
\itemize{
\item \code{ddf} a detection function model object.
\item \code{dht} abundance/density information (if survey region data was supplied,
else \code{NULL})
}
}
\description{
Fits a detection function (either point or line transect) to model detection radius or angle.
}
\examples{
data("ctdp")
observations <- ctdp$data$observations \%>\%
  dplyr::filter(scientificName == "Vulpes vulpes")

ct_fit_detmodel(radius ~ 1, data = observations)

# For angle
ct_fit_detmodel(angle ~ 1, data = observations)

}
\seealso{
\code{\link[=ct_fit_rem]{ct_fit_rem()}}, \code{\link[=ct_fit_speedmodel]{ct_fit_speedmodel()}}, \code{\link[=ct_fit_activity]{ct_fit_activity()}}
}
