% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct_rest_data.R
\name{ct_rest_stay}
\alias{ct_rest_stay}
\title{Prepare staying-time data for REST}
\usage{
ct_rest_stay(
  detection_data,
  station_data,
  station_column = "Station",
  species_column = "Species",
  stay_column = "Stay",
  censor_column = "Cens"
)
}
\arguments{
\item{detection_data}{A data frame with one row per video / detection.}

\item{station_data}{A data frame with one row per camera station.}

\item{station_column, species_column}{Columns giving the station ID and species
in \code{detection_data}.}

\item{stay_column}{Column holding the staying time in seconds.}

\item{censor_column}{Column holding the censoring flag (1 = censored,
0 = fully observed).}
}
\value{
A tibble with columns \code{Station}, \code{Species}, \code{Stay}, \code{Cens} plus any
station covariates, ready for \code{\link[=ct_fit_rest]{ct_fit_rest()}}.
}
\description{
Selects and standardises the staying-time and censoring columns from a
detection table and attaches per-station covariates.
}
\examples{
data(rest_detection)
data(rest_station)

# Column names can be strings, bare names, or positions:
stay <- ct_rest_stay(rest_detection, rest_station, stay_column = Stay)
head(stay)
}
