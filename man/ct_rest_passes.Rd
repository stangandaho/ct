% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct_rest_data.R
\name{ct_rest_passes}
\alias{ct_rest_passes}
\title{Aggregate the number of animal passes per station for REST / RAD-REST}
\usage{
ct_rest_passes(
  detection_data,
  station_data,
  station_column = "Station",
  species_column = "Species",
  passes_column = "y",
  model = c("REST", "RAD-REST")
)
}
\arguments{
\item{detection_data}{A data frame with one row per video / detection.}

\item{station_data}{A data frame with one row per camera station.}

\item{station_column, species_column}{Columns giving the station ID and species
in \code{detection_data}.}

\item{passes_column}{Column holding the number of passes per video.}

\item{model}{Either \code{"REST"} (totals as \code{Y}) or \code{"RAD-REST"} (per-video pass
counts spread into \code{y_0}, \code{y_1}, ... plus the video total \code{N}).}
}
\value{
A tibble with one row per station x species, ready for \code{\link[=ct_rest_effort]{ct_rest_effort()}}.
}
\description{
Aggregate the number of animal passes per station for REST / RAD-REST
}
\examples{
data(rest_detection)
data(rest_station)

# Original REST: total passes (Y) per station
ct_rest_passes(rest_detection, rest_station, model = "REST")

# RAD-REST: videos split by number of passes (y_0, y_1, ...)
ct_rest_passes(rest_detection, rest_station, model = "RAD-REST")

}
