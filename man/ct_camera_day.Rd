% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct_camera_day.R
\name{ct_camera_day}
\alias{ct_camera_day}
\title{Calculate daily camera trap captures}
\usage{
ct_camera_day(
  data,
  deployment_data = NULL,
  deployment_column,
  datetime_column,
  species_column,
  size_column,
  format,
  start_column = NULL,
  end_column = NULL,
  deployment_format = format,
  time_zone = ""
)
}
\arguments{
\item{data}{A data frame containing camera trap observation data.}

\item{deployment_data}{A data frame containing camera trap deployment records.}

\item{deployment_column}{The column name (unquoted or as a string) that uniquely identifies the deployment (e.g., camera ID).}

\item{datetime_column}{Column in \code{data} containing observation timestamps.
Can be supplied as a bare name, quoted string, or column position.}

\item{species_column}{The column in the data frame representing species identifiers. Can be specified as a string or unquoted column name.}

\item{size_column}{(Optional) The column representing the size or abundance of the species at each site. If not provided, counts of species occurrences are calculated.}

\item{format}{Character string specifying the datetime format for parsing
\code{datetime_column}.}

\item{start_column}{The column name (unquoted or as a string) indicating deployment start datetime.}

\item{end_column}{The column name (unquoted or as a string) indicating deployment end datetime.}

\item{deployment_format}{Character string specifying the datetime format for parsing
\code{start_column} and \code{end_column}. Defaults to the same format as \code{format}.}

\item{time_zone}{The time zone used to parse the datetime columns. Default is \code{""} (i.e., system time zone).}
}
\value{
A tibble with the following columns:
\itemize{
\item \code{deployment_column} (camera/location identifier)
\item \code{date} (Date of observation)
\item \code{species_column} (species name)
\item \code{size_column} (daily count, \code{0} if no observations)
\item \code{sampling_unit} (unique identifier for location × date combination)
}
}
\description{
Aggregates camera trap data into daily capture summaries.
}
\examples{
# Example observation data
obs <- data.frame(
  species = c("Deer", "Deer", "Fox", "Deer"),
  count = c(2, 1, 1, 3),
  datetime = c("2023-06-01 08:12:00", "2023-06-01 15:30:00",
               "2023-06-01 21:10:00", "2023-06-02 06:45:00"),
  location_id = c("Cam1", "Cam1", "Cam1", "Cam1"),
  stringsAsFactors = FALSE
)

# Example deployment data
dep <- data.frame(
  location_id = c("Cam1"),
  deploy_start = "2023-06-01 00:00:00",
  deploy_end = "2023-06-03 23:59:59",
  stringsAsFactors = FALSE
)

ct_camera_day(
  data = obs,
  deployment_data = dep,
  datetime_column = "datetime",
  species_column = "species",
  size_column = "count",
  deployment_column = "location_id",
  format = "\%Y-\%m-\%d \%H:\%M:\%S",
  start_column = "deploy_start",
  end_column = "deploy_end"
)

}
\seealso{
\code{\link[=ct_inext]{ct_inext()}}, \code{\link[=ct_get_effort]{ct_get_effort()}}
}
