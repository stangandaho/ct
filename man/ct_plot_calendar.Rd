% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct_plot_calendar.R
\name{ct_plot_calendar}
\alias{ct_plot_calendar}
\title{Plot a calendar heatmap of daily camera trap activity}
\usage{
ct_plot_calendar(
  data,
  datetime,
  format = NULL,
  size_column = NULL,
  only_month = NULL,
  fit_distribution = FALSE,
  abbreviate_month_name = FALSE,
  month_name = NULL,
  day_name = NULL,
  number_of_column = 4,
  low = NULL,
  high = NULL,
  palette = NULL,
  na_value = "grey95",
  show_day_number = TRUE,
  title = NULL
)
}
\arguments{
\item{data}{A data frame of records, one row per detection.}

\item{datetime}{Column holding the date or date-time of each record.}

\item{format}{Optional date format(s) passed to \code{\link[=as.Date]{as.Date()}} via \code{tryFormats}.
If \code{NULL} (default), a set of common date and date-time formats is tried.}

\item{size_column}{Optional column whose values are summed per day, for
example the number of individuals recorded in each detection. If omitted,
the number of records (detections) per day is used instead.}

\item{only_month}{Optional integer vector of month numbers (1 to 12) to keep,
for example \code{3:5}. Records outside these months are dropped and only those
month panels are drawn. Default \code{NULL} (the whole year).}

\item{fit_distribution}{Logical. If \code{TRUE}, a count distribution is fitted to
the records per day over the displayed period (days with no record count as
zeros) with \code{\link[=ct_fit_distribution]{ct_fit_distribution()}}, and the fitted distribution is reported
in the plot subtitle. Tiles are then shaded by the fitted density, that is
the probability of each day's count under the model. The calendar therefore
becomes a typicality map, not an activity map: the brightest tiles are the
most probable days under the fitted distribution, which for zero-heavy
camera-trap data are usually the days with no detection, while busier days
carry rarer counts and appear darker. See Details. Default \code{FALSE}.}

\item{abbreviate_month_name}{Logical. Use three-letter month names. Ignored
when \code{month_name} is supplied. Default \code{FALSE}.}

\item{month_name}{Optional length-12 character vector of month labels, for
localisation. Defaults to the English month names.}

\item{day_name}{Optional length-7 character vector of weekday labels, Monday
first. Defaults to \code{c("Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun")}.}

\item{number_of_column}{Number of month panels per row. Default \code{4}.}

\item{low, high}{Optional start and end colours for a two-colour gradient
fill. When both are supplied they override \code{palette}.}

\item{palette}{Optional fill palette. Either a single viridis option letter
(for example \code{"C"}), or a vector of two or more colours for a custom
gradient. Default \code{NULL} (viridis option C).}

\item{na_value}{Fill colour for days with no records. Default \code{"grey95"}.}

\item{show_day_number}{Logical. Print the day number inside each tile.
Default \code{TRUE}.}

\item{title}{Optional plot title. Generated automatically when \code{NULL}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Visualises a year of camera-trap records as a calendar heatmap.
Tiles are shaded by the number of
records per day, or by the summed value of a chosen column. A count
distribution can optionally be fitted to the daily values and used for the
shading.
}
\details{
With \code{fit_distribution = FALSE} the calendar is an activity map: tiles are
shaded by the records per day (or the summed \code{size_column}), so busier days
are brighter.

With \code{fit_distribution = TRUE} the calendar is instead a typicality map. A
single distribution is fitted to the whole displayed period, and each tile is
shaded by the fitted probability of that day's count. Because most days have
no detection, the count of zero is the most probable value, so empty days
receive the highest density and the brightest colour, while the rarer busy
days appear darker. The map highlights how typical or
unusual each day is under the model, rather than how much activity occurred.
Use \code{fit_distribution = FALSE} if you want activity intensity instead.
}
\examples{
library(dplyr)
data(ACBR)

# The calendar covers one year at a time, so keep a single year.
d2024 <- ACBR$acbr_data \%>\%
  # Filter to independent (10min separated) detections
  ct_independence(species_column = species,
                  datetime = datetime,
                  format = "\%Y-\%m-\%d \%H:\%M:\%S",
                  threshold = 10*60
  ) \%>\%
  # Select data for 2025 year
  filter(lubridate::year(datetime) == 2025)

ct_plot_calendar(d2024, datetime = datetime,
                 size_column = count,
                 low = "gray", high = "red"
)
#'
}
\seealso{
\code{\link[=ct_fit_distribution]{ct_fit_distribution()}}, \code{\link[=ct_plot_camtrap_activity]{ct_plot_camtrap_activity()}}
}
